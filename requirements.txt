# Framework
django==5.2.14
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1

# DB
psycopg[binary]==3.1.19

# OpenAPI / Swagger
drf-spectacular==0.27.2

# Config
python-decouple==3.8

# CORS
django-cors-headers==4.3.1

# HTTP client (pour run_checks et webhooks)
requests==2.32.3

# CLI doctor
typer==0.12.3
rich==13.7.1

# Production
gunicorn==22.0.0

# Dev / tests
pytest==8.2.2
pytest-django==4.8.0
pytest-cov==5.0.0

# Lint
ruff==0.4.10
black==24.4.2

# others
python-decouple==3.8

dj-database-url==3.1.2

# sudo docker compose -f docker/docker-compose-dev.yml exec db psql -U oneuptime -d oneuptime -c "\l"  ---> Vérifier l'existence de la BA dans docker

# sudo docker compose -f docker/docker-compose.dev.yml down -v ---> Arrêter et supprimer le container ET le volume de données

# sudo docker compose -f docker/docker-compose.dev.yml up -d  ----> Relancer (recrée tout depuis zéro)

# python -c "import secrets; print(secrets.token_urlsafe(50))" ---> Génerer clé secret 