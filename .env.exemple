# Django
DJANGO_SECRET_KEY=change-me-in-production-use-secrets-token-urlsafe-50
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1


POSTGRES_DB=oneuptime
POSTGRES_USER=oneuptime
POSTGRES_PASSWORD='up*dbPa1'
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Django (construit à partir des variables ci-dessus)
DATABASE_URL=postgres:POSTGRES_USER//:POSTGRES_PASSWORD@POSTGRES_HOST:POSTGRES_PORT/POSTGRES_DB
# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# JWT
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=15
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Email (console backend en dev)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=yaomariussodokin@gmail.com

# Webhooks
WEBHOOK_DEFAULT_TIMEOUT_SECONDS=5
WEBHOOK_MAX_RETRIES=3
EOF